# English Review — 每日个性化复习规范

## 目标

- 来源：ChatGPT 项目 `englishTranning`，稳定项目 ID：`g-p-6a2e2fef02c88191891509bb5366a438`。
- 用户水平：B1；每天 8–12 分钟。
- 每次定时运行必须把完整复习内容显示在本次任务的最终回复里，同时归档到 `reviews/YYYY-MM-DD.md`。
- 同时生成 `reviews/YYYY-MM-DD.audio.json`，供本地声音卡片播放页使用。每张卡只写需要朗读的自然英文答案/例句，不写中文解释、IPA 或语法分析；为连读题提供慢速分块文本和正常语速文本。
- 原 ChatGPT 项目保存原始口语练习；本目录只保存整理后的生词、地道表达、常犯错误、听力难点和复习状态。

## 每次运行流程

1. 完整读取本文件和 `ledger.json`，使用 `Asia/Shanghai` 的当前日期。在 Windows PowerShell 中读取这两个 UTF-8 文件时显式使用 `-Encoding UTF8`。
2. 尝试同步 ChatGPT 来源项目：
   - 使用可用的项目/聊天读取能力找到稳定项目 ID，而不是只依赖项目名拼写。
   - 只读取 `last_seen_updated_at` 之后新增或更新的项目聊天；必要时沿分页游标读到最老页。
   - 提取用户明确询问或不会的词、短语、地道改写、有证据的语法错误、发音和听辨问题。
   - 保留来源聊天和用户原句；按 `normalized_key` 去重。同一错误再次出现时增加 `occurrences`。
   - 旧回复只用于提供上下文，不把未经核对的旧解释当作事实。
   - 明显的语音转写乱码放入 `pending_confirmation`，不算语法错误。
3. 如果本次运行没有来源项目读取能力或同步失败：
   - 不得编造新增内容，也不要让复习任务失败。
   - 继续使用本地 `ledger.json` 生成复习；在末尾用一句话标明“本次使用本地总账，远程练习尚未同步”。
4. 选择 6–9 个复习点：到期项优先，其次是高频错误、低正确率项目和较久未出现的项目。每天最多引入 2 个全新项目。
5. 只把“展示过”记录为 `last_shown`；用户未作答时，不得增加 `correct` 或擅自标记“已掌握”。
6. 生成后保存 `reviews/YYYY-MM-DD.md` 和同日期 `.audio.json`，并更新 `ledger.json` 的同步游标、`last_generated_date`、`last_shown`。当天文件已存在且完整时，直接展示，避免重复生成。

## 每日输出结构

不要使用表格。按以下顺序输出：

1. `☀️ 今日口语复习｜YYYY-MM-DD`
2. `先别看答案`：
   - 3–4 个中译英或释义回忆题
   - 2 个原句纠错题
   - 1 个听力/连读辨音题
3. `地道表达替换`：给 1–2 个中文意图，让用户口头说英语。
4. `口语挑战`：一个 30–60 秒、与用户真实经历相关的话题，要求至少使用 2 个今日复习项。
5. `答案与提示`：与题目一一对应；明确“错误”与“没错但可更自然”的区别。
6. `今天最值得记住的一件事`：只保留一条。

## 纠错和间隔复习规则

- 优先纠正会影响理解、重复出现或可迁移性强的问题；一次最多精讲 3 类错误。
- 答对后的间隔依次建议为 3、7、14、30 天；答错或明确不会，次日再复习。
- 相同含义的词要讲清使用边界，不只给中文同义词。
- 问词义时使用三步法：简明中文义 → 自然英文定义 → 贴近用户经历的英文例句。
- `comment` 是代码注释；`comment out` 是把代码注释掉使其不执行；`noted` 是“知道了/已记下”。

## 当前重点

- `insist on` 与 `persist in`
- `resist`、`reject` 与 `push back against`
- `What does X mean?` 的问句结构
- `repeat again` 的冗余
- 双主语、缺少 `be`、第三人称单数
- 名词/形容词混用
- `was about to` 与 `hit you` 的弱读、连读和合音
